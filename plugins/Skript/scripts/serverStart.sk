   on load:
       set {difficulty.changed} to false
       
   on server start:
       if {difficulty.changed} is false:
           execute console command "difficulty <<!nav>>normal<<!/nav>>" # Replace with your desired difficulty
           set {difficulty.changed} to true
           
   on server stop:
       set {difficulty.changed} to false